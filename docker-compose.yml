version: '3.8'
services:
  gateway:
    build: ./nginx
    ports:
      - "8080:8080"
    environment:
      # 设置你的后端地址，例如 http://backend:3000 或 https://api.example.com
      - VITE_API_URL=${VITE_API_URL:-http://backend:3000}
